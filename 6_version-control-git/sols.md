1. Did a few Learn Git Branching exercises.
2.

- `git log --all --graph --decorate --oneline`
- `git log README.md` Anish!
- `git blame _config.yml` commit hash: `a88b4eac`
- `git log a88b4eac` "Redo lectures as a collection"

3. Installed BFS so I have it handy in the future.
4. `git log --all` includes the stash log
5. Aliased `git graph`.
6. Made a `~/.gitignore_global`.
7. Don't wanna spam the class website PRs!
